pipeline {
  agent any
  stages {
    stage('Say Hello') {
      steps {
        echo 'Hello World!'   
        sh 'java -version'
        sh 'df -hP'
        sh 'uname -a'
        sh 'free -g'
        sh 'nproc'
      }
    }
  }
}
